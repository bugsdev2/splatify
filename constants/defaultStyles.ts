import { StyleSheet } from 'react-native';

export const defaultStyleSheet = StyleSheet.create({
    container: {
        flex: 1,
    },

    button: {
        //
    },

    buttonText: {
        //
    },
});
